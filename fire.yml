# ~/.tmuxinator/fire.yml
# you can make as many tabs as you wish...

project_name: Fire
project_root: '~'
#socket_name: foo # Not needed.  Remove to use default socket
#rvm: 1.9.2@rails_project
#pre: sudo /etc/rc.d/foo start
tabs:
  - admin:
      layout: tiled
      panes:
        - ssh sromain
        - ssh fjames
        - ssh jjameson
        - ssh kkassin
  - local:
      layout: tiled
      panes:
        - htop
        - #empty, will just run plain bash
  - foo: ls